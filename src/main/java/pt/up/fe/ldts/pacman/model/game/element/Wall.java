package pt.up.fe.ldts.pacman.model.game.element;

import pt.up.fe.ldts.pacman.model.game.Position;

public class Wall extends Element implements StaticElement {
    public Wall(Position pos) {
        super(pos);
    }
}
